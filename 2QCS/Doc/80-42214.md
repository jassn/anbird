# 80-42214-1 BLP
Qualcomm Aware™ Positioning Service Beacon Location Provider for Android

- [ ] adb remount

For Android 13 and later versions, push the xxx.conf file to
/system_ext/etc directory instead of system.

Create a subdirectory in the external folder.

Create Android.mk makefile.

For Android, add the package to PRODUCT_PACKAGES in the Vendor/build/make/target/product/core.mk directory.

start or stop Android service
```
adb shell am start-service -n xyzService
```



== END ==
